import styled from "@emotion/styled"

const Styled = {
    Title: styled.h1`
        margin-bottom: 40px;
    `,

List: styled.ul`
    display: flex;
    flex-wrap: wrap;  
    gap: 24px;
    margin-top: 40px;
`
}

export default Styled